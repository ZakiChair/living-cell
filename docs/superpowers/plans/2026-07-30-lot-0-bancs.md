# Lot 0 — Les trois bancs bloquants : plan d'implémentation

> **Pour les agents d'exécution :** SOUS-COMPÉTENCE REQUISE — utiliser `superpowers:subagent-driven-development` (recommandé) ou `superpowers:executing-plans` pour implémenter ce plan tâche par tâche. Les étapes utilisent la syntaxe à cases à cocher (`- [ ]`) pour le suivi.

**But :** répondre par des mesures à trois questions qui bloquent toute la conception du site « Cellule vivante » — le contour du cel-shading est-il finançable, une dalle sub-micronique à densité vraie tient-elle le budget sur ordinateur et sur téléphone, et combien coûte réellement la production d'une silhouette moléculaire.

**Architecture :** un projet Vite + TypeScript + Three.js dont chaque banc est une page HTML autonome. Le cœur mathématique de chaque banc (conversion d'échelles, calcul de densité, formule de contour) vit dans `src/noyau/`, ne dépend d'aucune API graphique, et est couvert par Vitest. Le harnais visuel vit dans `bancs/` et produit des chiffres affichés à l'écran plus un fichier de résultats.

**Pile technique :** Vite 8.2.0, TypeScript 7.0.2, Three.js 0.185.1, Vitest 4.1.10. Aucune autre dépendance dans ce lot.

## Contraintes globales

Ces règles s'appliquent à chaque tâche. Valeurs reprises telles quelles de la spec.

- **Français** pour tout : noms de fichiers, identifiants, commentaires, messages de commit, texte affiché.
- **Versions épinglées exactement**, sans accent circonflexe ni tilde : `three@0.185.1`, `vite@8.2.0`, `typescript@7.0.2`, `vitest@4.1.10`. Three.js est versionné en `0.x` avec des ruptures possibles à chaque publication mensuelle.
- **`src/noyau/` n'importe jamais `three` ni le DOM.** C'est ce qui rend les tests exécutables sans GPU. Un import de `three` dans `src/noyau/` est un échec de revue.
- **Jamais `THREE.Points`.** Safari sur Apple Silicon plafonne `gl_PointSize` à 64 px et WebGPU ne dimensionne pas les points. Toute particule visible est un quad instancié ou un `InstancedMesh`.
- **Aucune allocation dans la boucle de rendu.** Pas de `new Vector3()`, pas de `new Matrix4()`, pas de création de matériau par image. Les objets temporaires sont hissés au niveau du module.
- **Cibles chiffrées** que les bancs doivent vérifier : **≥ 55 images/s** sur le Mac de développement, **≥ 28 images/s** sur l'iPhone cible, sur la scène la plus dense.
- **Budget d'image** : 16,7 ms au total sur ordinateur, dont environ 12 ms de GPU et 4 ms de fil principal.
- **Référence de coût déjà mesurée**, à ne pas re-mesurer : bloom +0,30 ms, profondeur de champ +5,40 ms, SSAO +5,63 ms, sur une scène de base à 2,27 ms. Le contour concourt au même budget.

---

## Structure des fichiers

```
cellule-vivante/
├── package.json                     Tâche 1
├── tsconfig.json                    Tâche 1
├── vite.config.ts                   Tâche 1
├── index.html                       Tâche 1  — sommaire des bancs
├── src/
│   └── noyau/                       PUR : zéro import three, zéro DOM
│       ├── echelles.ts              Tâche 2  — nanomètres ↔ unités de scène
│       ├── echelles.test.ts         Tâche 2
│       ├── metrologie.ts            Tâche 3  — moyenne glissante + hystérésis
│       ├── metrologie.test.ts       Tâche 3
│       ├── contour.ts               Tâche 5  — largeur de contour à l'écran
│       ├── contour.test.ts          Tâche 5
│       ├── densite.ts               Tâche 8  — remplissage d'une dalle
│       ├── densite.test.ts          Tâche 8
│       ├── pdb.ts                   Tâche 11 — analyse d'un fichier PDB
│       └── pdb.test.ts              Tâche 11
├── bancs/
│   ├── harnais.ts                   Tâche 4  — boucle, mesure, affichage
│   ├── 0a-contour.html              Tâche 5
│   ├── 0a-contour.ts                Tâches 5 et 6
│   ├── 0b-dalle.html                Tâche 9
│   ├── 0b-dalle.ts                  Tâche 9
│   └── resultats/                   fichiers de mesures produits par les bancs
├── outils/
│   └── silhouette.ts                Tâche 11 — PDB → binaire
└── docs/superpowers/
    ├── specs/2026-07-30-cellule-vivante-design.md   (existe)
    └── rapports/lot-0.md            Tâche 12 — les trois verdicts
```

Chaque module de `src/noyau/` a une seule responsabilité et tient sous 150 lignes. Les tests vivent à côté du module qu'ils couvrent.

---

## Tâche 1 : Socle du projet

**Fichiers :**
- Créer : `package.json`, `tsconfig.json`, `vite.config.ts`, `index.html`
- Créer : `src/noyau/version.ts`
- Test : `src/noyau/version.test.ts`

**Interfaces :**
- Consomme : rien.
- Produit : `npm test` exécute Vitest, `npm run banc` lance Vite. La constante `VERSION_TROIS` vaut `'0.185.1'` et sert de garde-fou contre une montée de version accidentelle.

- [ ] **Étape 1 : Installer les dépendances aux versions exactes**

```bash
cd ~/Projects/cellule-vivante
npm init -y
npm install --save-exact three@0.185.1
npm install --save-dev --save-exact vite@8.2.0 typescript@7.0.2 vitest@4.1.10 @types/three@0.185.0
```

- [ ] **Étape 2 : Écrire les fichiers de configuration**

`package.json` — remplacer le bloc `scripts` généré par :

```json
  "type": "module",
  "scripts": {
    "banc": "vite",
    "build": "vite build",
    "test": "vitest run",
    "test:suivi": "vitest"
  },
```

`tsconfig.json` :

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noEmit": true,
    "skipLibCheck": true,
    "types": ["vite/client"]
  },
  "include": ["src", "bancs", "outils"]
}
```

`vite.config.ts` :

```ts
import { defineConfig } from 'vite'
import { resolve } from 'node:path'

// Chaque banc est une page autonome : on les déclare toutes en points d'entrée.
export default defineConfig({
  build: {
    rollupOptions: {
      input: {
        sommaire: resolve(__dirname, 'index.html'),
        contour: resolve(__dirname, 'bancs/0a-contour.html'),
        dalle: resolve(__dirname, 'bancs/0b-dalle.html'),
      },
    },
  },
})
```

`index.html` :

```html
<!doctype html>
<html lang="fr">
  <head>
    <meta charset="utf-8" />
    <title>Cellule vivante — bancs du lot 0</title>
    <style>
      body { font: 16px/1.6 system-ui, sans-serif; max-width: 40rem; margin: 4rem auto; padding: 0 1rem; }
      h1 { font-size: 1.4rem; }
      li { margin: 0.5rem 0; }
    </style>
  </head>
  <body>
    <h1>Bancs du lot 0</h1>
    <p>Trois mesures qui débloquent la conception. Chaque banc affiche ses chiffres à l'écran.</p>
    <ul>
      <li><a href="/bancs/0a-contour.html">0a — Contour du cel-shading</a></li>
      <li><a href="/bancs/0b-dalle.html">0b — Dalle à densité vraie</a></li>
    </ul>
  </body>
</html>
```

- [ ] **Étape 3 : Écrire le test qui échoue**

`src/noyau/version.test.ts` :

```ts
import { describe, it, expect } from 'vitest'
import { VERSION_TROIS } from './version.js'

describe('garde-fou de version', () => {
  it('épingle Three.js à la version mesurée dans les bancs', () => {
    expect(VERSION_TROIS).toBe('0.185.1')
  })
})
```

- [ ] **Étape 4 : Lancer le test et vérifier qu'il échoue**

Lancer : `npm test`
Attendu : ÉCHEC, `Failed to resolve import "./version.js"`.

- [ ] **Étape 5 : Écrire l'implémentation minimale**

`src/noyau/version.ts` :

```ts
/** Version de Three.js sur laquelle tous les chiffres du lot 0 ont été mesurés. */
export const VERSION_TROIS = '0.185.1'
```

- [ ] **Étape 6 : Lancer le test et vérifier qu'il passe**

Lancer : `npm test`
Attendu : SUCCÈS, 1 test.

- [ ] **Étape 7 : Vérifier que Vite démarre**

Lancer : `npm run banc`
Attendu : le serveur démarre et `http://localhost:5173/` affiche le sommaire. Arrêter avec Ctrl-C.

- [ ] **Étape 8 : Commiter**

```bash
git add -A
git commit -m "Lot 0 : socle Vite + TypeScript + Three.js aux versions épinglées"
```

---

## Tâche 2 : Conversion des échelles

Le site couvre trois ordres de grandeur. Toute erreur d'échelle rend les mesures des bancs suivants ininterprétables, donc cette conversion est isolée et testée avant tout rendu.

**Fichiers :**
- Créer : `src/noyau/echelles.ts`
- Test : `src/noyau/echelles.test.ts`

**Interfaces :**
- Consomme : rien.
- Produit :
  - `RAYON_SCENE: number` — demi-largeur du champ visible, en unités de scène, identique pour les trois bandes.
  - `type Bande = 'cellule' | 'boite' | 'macromolecule'`
  - `CHAMPS_NM: Record<Bande, number>` — largeur du champ visible de chaque bande, en nanomètres.
  - `unitesParNm(champNm: number): number`
  - `nmVersUnites(nm: number, champNm: number): number`
  - `unitesVersNm(unites: number, champNm: number): number`

- [ ] **Étape 1 : Écrire les tests qui échouent**

`src/noyau/echelles.test.ts` :

```ts
import { describe, it, expect } from 'vitest'
import { CHAMPS_NM, RAYON_SCENE, nmVersUnites, unitesVersNm, unitesParNm } from './echelles.js'

describe('champs des trois bandes', () => {
  it('couvre trois ordres de grandeur, de la cellule à la macromolécule', () => {
    expect(CHAMPS_NM.cellule).toBe(20_000)
    expect(CHAMPS_NM.boite).toBe(1_000)
    expect(CHAMPS_NM.macromolecule).toBe(25)
  })
})

describe('conversion nanomètres vers unités de scène', () => {
  it('fait tenir le champ entier dans le diamètre de la scène', () => {
    expect(nmVersUnites(CHAMPS_NM.boite, CHAMPS_NM.boite)).toBeCloseTo(RAYON_SCENE * 2, 10)
  })

  it('rend un ribosome de 25 nm plein cadre dans la bande macromolécule', () => {
    expect(nmVersUnites(25, CHAMPS_NM.macromolecule)).toBeCloseTo(RAYON_SCENE * 2, 10)
  })

  it('rend le même ribosome 800 fois plus petit dans la bande cellule', () => {
    const dansMacro = nmVersUnites(25, CHAMPS_NM.macromolecule)
    const dansCellule = nmVersUnites(25, CHAMPS_NM.cellule)
    expect(dansMacro / dansCellule).toBeCloseTo(800, 6)
  })

  it('garde une membrane de 5 nm visible dans la bande boîte', () => {
    // 5 nm sur un champ de 1000 nm = 0,5 % du champ : fin mais dessinable.
    expect(nmVersUnites(5, CHAMPS_NM.boite)).toBeCloseTo(RAYON_SCENE * 2 * 0.005, 10)
  })
})

describe('aller-retour', () => {
  it('revient à la valeur de départ pour chaque bande', () => {
    for (const champ of Object.values(CHAMPS_NM)) {
      expect(unitesVersNm(nmVersUnites(137, champ), champ)).toBeCloseTo(137, 6)
    }
  })
})

describe('facteur d’échelle', () => {
  it('est inversement proportionnel à la largeur du champ', () => {
    expect(unitesParNm(1_000) / unitesParNm(20_000)).toBeCloseTo(20, 10)
  })

  it('refuse un champ nul ou négatif plutôt que de renvoyer l’infini', () => {
    expect(() => unitesParNm(0)).toThrow(/champ/i)
    expect(() => unitesParNm(-5)).toThrow(/champ/i)
  })
})
```

- [ ] **Étape 2 : Lancer les tests et vérifier qu'ils échouent**

Lancer : `npx vitest run src/noyau/echelles.test.ts`
Attendu : ÉCHEC, `Failed to resolve import "./echelles.js"`.

- [ ] **Étape 3 : Écrire l'implémentation**

`src/noyau/echelles.ts` :

```ts
/**
 * Conversion entre nanomètres réels et unités de scène.
 *
 * Chaque bande d'échelle ramène son champ visible au même diamètre de scène.
 * On garde ainsi la caméra, les distances de coupe et les tolérances numériques
 * identiques d'une bande à l'autre : seule la correspondance change.
 */

/** Demi-largeur du champ visible, en unités de scène. Identique pour les trois bandes. */
export const RAYON_SCENE = 10

export type Bande = 'cellule' | 'boite' | 'macromolecule'

/** Largeur du champ visible de chaque bande, en nanomètres. */
export const CHAMPS_NM: Record<Bande, number> = {
  cellule: 20_000,
  boite: 1_000,
  macromolecule: 25,
}

/** Unités de scène par nanomètre, pour un champ visible donné. */
export function unitesParNm(champNm: number): number {
  if (!(champNm > 0)) {
    throw new Error(`champ visible invalide : ${champNm} nm (attendu strictement positif)`)
  }
  return (RAYON_SCENE * 2) / champNm
}

export function nmVersUnites(nm: number, champNm: number): number {
  return nm * unitesParNm(champNm)
}

export function unitesVersNm(unites: number, champNm: number): number {
  return unites / unitesParNm(champNm)
}
```

- [ ] **Étape 4 : Lancer les tests et vérifier qu'ils passent**

Lancer : `npx vitest run src/noyau/echelles.test.ts`
Attendu : SUCCÈS, 8 tests.

- [ ] **Étape 5 : Commiter**

```bash
git add src/noyau/echelles.ts src/noyau/echelles.test.ts
git commit -m "Lot 0 : conversion des échelles entre nanomètres et unités de scène"
```

---

## Tâche 3 : Métrologie du temps d'image

Tous les bancs mesurent la même chose. Le compteur est isolé et testé sur des séquences synthétiques, parce qu'un compteur faux invaliderait les trois verdicts.

L'hystérésis n'est pas un raffinement : sans elle, une scène qui oscille autour d'un seuil bascule de niveau de qualité à chaque image et devient inutilisable.

**Fichiers :**
- Créer : `src/noyau/metrologie.ts`
- Test : `src/noyau/metrologie.test.ts`

**Interfaces :**
- Consomme : rien.
- Produit :
  - `type Verdict = 'degrader' | 'stable' | 'ameliorer'`
  - `creerCompteur(options?: { taille?: number, seuilHaut?: number, seuilBas?: number, patience?: number }): Compteur`
  - `interface Compteur { ajouter(ms: number): void; moyenne(): number; imagesParSeconde(): number; verdict(): Verdict; nombreEchantillons(): number }`

- [ ] **Étape 1 : Écrire les tests qui échouent**

`src/noyau/metrologie.test.ts` :

```ts
import { describe, it, expect } from 'vitest'
import { creerCompteur } from './metrologie.js'

/** Injecte n fois la même durée d'image. */
function alimenter(compteur: ReturnType<typeof creerCompteur>, ms: number, n: number): void {
  for (let i = 0; i < n; i++) compteur.ajouter(ms)
}

describe('moyenne glissante', () => {
  it('vaut zéro tant qu’aucun échantillon n’est arrivé', () => {
    expect(creerCompteur().moyenne()).toBe(0)
    expect(creerCompteur().nombreEchantillons()).toBe(0)
  })

  it('moyenne les échantillons reçus', () => {
    const c = creerCompteur({ taille: 4 })
    c.ajouter(10)
    c.ajouter(20)
    expect(c.moyenne()).toBeCloseTo(15, 10)
  })

  it('oublie les échantillons au-delà de sa fenêtre', () => {
    const c = creerCompteur({ taille: 3 })
    alimenter(c, 100, 3)
    alimenter(c, 10, 3)
    expect(c.moyenne()).toBeCloseTo(10, 10)
    expect(c.nombreEchantillons()).toBe(3)
  })

  it('convertit en images par seconde', () => {
    const c = creerCompteur({ taille: 4 })
    alimenter(c, 16.666, 4)
    expect(c.imagesParSeconde()).toBeCloseTo(60, 1)
  })

  it('renvoie zéro image par seconde sans échantillon plutôt que l’infini', () => {
    expect(creerCompteur().imagesParSeconde()).toBe(0)
  })
})

describe('verdict avec hystérésis', () => {
  it('reste stable tant que la fenêtre n’est pas pleine', () => {
    const c = creerCompteur({ taille: 8 })
    alimenter(c, 50, 3)
    expect(c.verdict()).toBe('stable')
  })

  it('demande à dégrader au-dessus du seuil haut', () => {
    const c = creerCompteur({ taille: 4 })
    alimenter(c, 25, 4)
    expect(c.verdict()).toBe('degrader')
  })

  it('demande à améliorer sous le seuil bas, mais seulement après la patience', () => {
    const c = creerCompteur({ taille: 4, patience: 10 })
    alimenter(c, 8, 4)
    expect(c.verdict()).toBe('stable')
    alimenter(c, 8, 6)
    expect(c.verdict()).toBe('ameliorer')
  })

  it('ne bascule pas entre les deux seuils', () => {
    const c = creerCompteur({ taille: 4, seuilHaut: 20, seuilBas: 12 })
    alimenter(c, 16, 4)
    expect(c.verdict()).toBe('stable')
  })

  it('remet la patience à zéro dès qu’une image dépasse le seuil bas', () => {
    const c = creerCompteur({ taille: 2, patience: 5 })
    alimenter(c, 8, 6)
    expect(c.verdict()).toBe('ameliorer')
    c.ajouter(30)
    c.ajouter(30)
    expect(c.verdict()).toBe('degrader')
    alimenter(c, 8, 2)
    expect(c.verdict()).toBe('stable')
  })
})
```

- [ ] **Étape 2 : Lancer les tests et vérifier qu'ils échouent**

Lancer : `npx vitest run src/noyau/metrologie.test.ts`
Attendu : ÉCHEC, `Failed to resolve import "./metrologie.js"`.

- [ ] **Étape 3 : Écrire l'implémentation**

`src/noyau/metrologie.ts` :

```ts
/**
 * Mesure du temps d'image et décision de dégradation.
 *
 * Le throttling thermique étant réel — une scène stable une minute peut perdre
 * 30 à 40 % après dix minutes — la mesure est continue, pas un test unique au
 * chargement. L'hystérésis empêche le niveau de qualité d'osciller.
 */

export type Verdict = 'degrader' | 'stable' | 'ameliorer'

export interface Compteur {
  ajouter(ms: number): void
  moyenne(): number
  imagesParSeconde(): number
  verdict(): Verdict
  nombreEchantillons(): number
}

export interface OptionsCompteur {
  /** Nombre d'images gardées dans la fenêtre glissante. */
  taille?: number
  /** Au-dessus de cette moyenne en millisecondes, on dégrade. */
  seuilHaut?: number
  /** En dessous de cette moyenne, on peut remonter d'un cran. */
  seuilBas?: number
  /** Nombre d'images consécutives sous le seuil bas avant de remonter. */
  patience?: number
}

export function creerCompteur(options: OptionsCompteur = {}): Compteur {
  const taille = options.taille ?? 60
  const seuilHaut = options.seuilHaut ?? 20
  const seuilBas = options.seuilBas ?? 12
  const patience = options.patience ?? 120

  const fenetre = new Float64Array(taille)
  let curseur = 0
  let remplissage = 0
  let somme = 0
  let imagesCalmes = 0

  return {
    ajouter(ms: number): void {
      if (remplissage === taille) somme -= fenetre[curseur]!
      else remplissage++
      fenetre[curseur] = ms
      somme += ms
      curseur = (curseur + 1) % taille

      if (ms < seuilBas) imagesCalmes++
      else imagesCalmes = 0
    },

    moyenne(): number {
      return remplissage === 0 ? 0 : somme / remplissage
    },

    imagesParSeconde(): number {
      const m = this.moyenne()
      return m === 0 ? 0 : 1000 / m
    },

    nombreEchantillons(): number {
      return remplissage
    },

    verdict(): Verdict {
      // Tant que la fenêtre n'est pas pleine, la moyenne n'est pas représentative.
      if (remplissage < taille) return 'stable'
      const m = somme / remplissage
      if (m > seuilHaut) return 'degrader'
      if (m < seuilBas && imagesCalmes >= patience) return 'ameliorer'
      return 'stable'
    },
  }
}
```

- [ ] **Étape 4 : Lancer les tests et vérifier qu'ils passent**

Lancer : `npx vitest run src/noyau/metrologie.test.ts`
Attendu : SUCCÈS, 10 tests.

- [ ] **Étape 5 : Commiter**

```bash
git add src/noyau/metrologie.ts src/noyau/metrologie.test.ts
git commit -m "Lot 0 : compteur de temps d'image avec hystérésis"
```

---

## Tâche 4 : Harnais commun des bancs

Les trois bancs partagent la même mécanique : une scène Three.js, une boucle qui mesure, un panneau qui affiche, un bouton qui exporte les résultats. On l'écrit une fois.

Le temps GPU est mesuré par `EXT_disjoint_timer_query_webgl2` quand l'extension est disponible, parce que le temps mur seul est plafonné par la synchronisation verticale : une scène qui affiche « 8,3 ms » consomme peut-être 2 ms. Sans cette distinction, les mesures de contour seraient inexploitables.

**Fichiers :**
- Créer : `bancs/harnais.ts`

**Interfaces :**
- Consomme : `creerCompteur` de `src/noyau/metrologie.ts`.
- Produit :
  - `interface ContexteBanc { renderer: THREE.WebGLRenderer; scene: THREE.Scene; camera: THREE.PerspectiveCamera; compteur: Compteur }`
  - `creerBanc(options: { titre: string }): ContexteBanc`
  - `demarrerBoucle(ctx: ContexteBanc, parImage: (dt: number) => void): void`
  - `definirLignes(lignes: string[]): void` — remplit le panneau de mesures.
  - `mesurerGpu(ctx: ContexteBanc): number | null` — millisecondes GPU de la dernière image mesurée, ou `null` si l'extension manque.
  - `enregistrerResultat(nom: string, donnees: unknown): void` — télécharge un JSON.

- [ ] **Étape 1 : Écrire le harnais**

`bancs/harnais.ts` :

```ts
import * as THREE from 'three'
import { creerCompteur, type Compteur } from '../src/noyau/metrologie.js'

export interface ContexteBanc {
  renderer: THREE.WebGLRenderer
  scene: THREE.Scene
  camera: THREE.PerspectiveCamera
  compteur: Compteur
}

let panneau: HTMLPreElement

/** Sondes de temps GPU en attente de résultat. */
const requetes: WebGLQuery[] = []
let extensionTimer: any = null
let dernierGpuMs: number | null = null

export function creerBanc(options: { titre: string }): ContexteBanc {
  document.title = options.titre

  const renderer = new THREE.WebGLRenderer({
    antialias: false,
    powerPreference: 'high-performance',
  })
  renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2))
  renderer.setSize(window.innerWidth, window.innerHeight)
  document.body.appendChild(renderer.domElement)

  // Sans ce gestionnaire, le mode de panne d'iOS est un canvas blanc silencieux.
  renderer.domElement.addEventListener('webglcontextlost', (e) => {
    e.preventDefault()
    definirLignes(['CONTEXTE WEBGL PERDU — dépassement mémoire probable'])
  })

  const scene = new THREE.Scene()
  scene.background = new THREE.Color('#F2EEE4')

  const camera = new THREE.PerspectiveCamera(50, window.innerWidth / window.innerHeight, 0.1, 500)
  camera.position.set(0, 0, 28)

  window.addEventListener('resize', () => {
    camera.aspect = window.innerWidth / window.innerHeight
    camera.updateProjectionMatrix()
    renderer.setSize(window.innerWidth, window.innerHeight)
  })

  panneau = document.createElement('pre')
  panneau.style.cssText =
    'position:fixed;top:0;left:0;margin:0;padding:12px 16px;' +
    'font:13px/1.5 ui-monospace,Menlo,monospace;color:#1C1A17;' +
    'background:rgba(242,238,228,.82);backdrop-filter:blur(6px);' +
    'white-space:pre;pointer-events:none'
  document.body.appendChild(panneau)

  const gl = renderer.getContext() as WebGL2RenderingContext
  extensionTimer = gl.getExtension('EXT_disjoint_timer_query_webgl2')

  return { renderer, scene, camera, compteur: creerCompteur({ taille: 60 }) }
}

export function definirLignes(lignes: string[]): void {
  if (panneau) panneau.textContent = lignes.join('\n')
}

export function mesurerGpu(): number | null {
  return dernierGpuMs
}

export function demarrerBoucle(ctx: ContexteBanc, parImage: (dt: number) => void): void {
  let precedent = performance.now()

  const boucle = (): void => {
    requestAnimationFrame(boucle)

    // Onglet masqué : on ne brûle pas de batterie et on ne pollue pas les mesures.
    if (document.visibilityState === 'hidden') {
      precedent = performance.now()
      return
    }

    const maintenant = performance.now()
    const dt = maintenant - precedent
    precedent = maintenant
    ctx.compteur.ajouter(dt)

    const gl = ctx.renderer.getContext() as WebGL2RenderingContext
    let requete: WebGLQuery | null = null
    if (extensionTimer) {
      requete = gl.createQuery()
      if (requete) gl.beginQuery(extensionTimer.TIME_ELAPSED_EXT, requete)
    }

    parImage(dt)
    ctx.renderer.render(ctx.scene, ctx.camera)

    if (extensionTimer && requete) {
      gl.endQuery(extensionTimer.TIME_ELAPSED_EXT)
      requetes.push(requete)
    }
    recolterGpu(gl)
  }

  requestAnimationFrame(boucle)
}

/** Relève les sondes GPU dont le résultat est arrivé. Non bloquant. */
function recolterGpu(gl: WebGL2RenderingContext): void {
  while (requetes.length > 0) {
    const q = requetes[0]!
    const prete = gl.getQueryParameter(q, gl.QUERY_RESULT_AVAILABLE)
    const invalide = gl.getParameter(extensionTimer.GPU_DISJOINT_EXT)
    if (!prete && !invalide) return
    requetes.shift()
    if (prete && !invalide) {
      dernierGpuMs = gl.getQueryParameter(q, gl.QUERY_RESULT) / 1e6
    }
    gl.deleteQuery(q)
  }
}

export function enregistrerResultat(nom: string, donnees: unknown): void {
  const blob = new Blob([JSON.stringify(donnees, null, 2)], { type: 'application/json' })
  const lien = document.createElement('a')
  lien.href = URL.createObjectURL(blob)
  lien.download = `${nom}.json`
  lien.click()
  URL.revokeObjectURL(lien.href)
}
```

- [ ] **Étape 2 : Vérifier que TypeScript accepte le fichier**

Lancer : `npx tsc --noEmit`
Attendu : aucune erreur.

- [ ] **Étape 3 : Commiter**

```bash
git add bancs/harnais.ts
git commit -m "Lot 0 : harnais commun des bancs avec mesure du temps GPU"
```

---

## Tâche 5 : Banc 0a — coque inversée à largeur constante à l'écran

La coque inversée dessine, derrière chaque instance, une copie légèrement gonflée en faces arrière et en couleur sombre. Le piège connu : l'offset est en espace objet, donc le contour est épais au premier plan et disparaît au fond. Il faut le corriger pour obtenir une largeur constante **en pixels**.

La correction est une formule pure, testée avant d'écrire le moindre shader.

**Fichiers :**
- Créer : `src/noyau/contour.ts`
- Test : `src/noyau/contour.test.ts`
- Créer : `bancs/0a-contour.html`
- Créer : `bancs/0a-contour.ts`

**Interfaces :**
- Consomme : `creerBanc`, `demarrerBoucle`, `definirLignes`, `mesurerGpu`, `enregistrerResultat` de `bancs/harnais.ts` ; `nmVersUnites`, `CHAMPS_NM` de `src/noyau/echelles.ts`.
- Produit :
  - `offsetContour(zVue: number, largeurPx: number, hauteurEcranPx: number, p11: number): number` — décalage à appliquer le long de la normale, en unités de scène.
  - `projection11(fovDegres: number): number` — élément `[1][1]` de la matrice de projection perspective.

- [ ] **Étape 1 : Écrire les tests qui échouent**

`src/noyau/contour.test.ts` :

```ts
import { describe, it, expect } from 'vitest'
import { offsetContour, projection11 } from './contour.js'

describe('élément [1][1] de la projection', () => {
  it('vaut 1 / tan(fov / 2)', () => {
    expect(projection11(90)).toBeCloseTo(1, 10)
    expect(projection11(50)).toBeCloseTo(1 / Math.tan((25 * Math.PI) / 180), 10)
  })

  it('décroît quand le champ de vision s’élargit', () => {
    expect(projection11(30)).toBeGreaterThan(projection11(90))
  })
})

describe('offset de contour à largeur constante à l’écran', () => {
  const p11 = projection11(50)

  it('croît proportionnellement à la distance', () => {
    const proche = offsetContour(-10, 2, 1000, p11)
    const loin = offsetContour(-20, 2, 1000, p11)
    expect(loin / proche).toBeCloseTo(2, 10)
  })

  it('croît proportionnellement à la largeur demandée', () => {
    const fin = offsetContour(-10, 1, 1000, p11)
    const epais = offsetContour(-10, 3, 1000, p11)
    expect(epais / fin).toBeCloseTo(3, 10)
  })

  it('décroît quand l’écran gagne en hauteur de pixels', () => {
    const petit = offsetContour(-10, 2, 500, p11)
    const grand = offsetContour(-10, 2, 1000, p11)
    expect(petit / grand).toBeCloseTo(2, 10)
  })

  it('donne une valeur positive pour un point devant la caméra', () => {
    // En espace vue, un objet visible a un z négatif.
    expect(offsetContour(-10, 2, 1000, p11)).toBeGreaterThan(0)
  })

  it('vaut la valeur attendue sur un cas de référence chiffré', () => {
    // z = -10, 2 px demandés, écran de 1000 px, champ de 50 degrés.
    // offset = 10 * 2 * 2 / (1000 * 2,14450692) = 0,01865...
    expect(offsetContour(-10, 2, 1000, p11)).toBeCloseTo(0.0186524, 6)
  })

  it('est nul si aucune largeur n’est demandée', () => {
    expect(offsetContour(-10, 0, 1000, p11)).toBe(0)
  })
})
```

- [ ] **Étape 2 : Lancer les tests et vérifier qu'ils échouent**

Lancer : `npx vitest run src/noyau/contour.test.ts`
Attendu : ÉCHEC, `Failed to resolve import "./contour.js"`.

- [ ] **Étape 3 : Écrire l'implémentation**

`src/noyau/contour.ts` :

```ts
/**
 * Géométrie du contour par coque inversée.
 *
 * Une coque gonflée d'une constante en espace objet donne un contour dont
 * l'épaisseur à l'écran varie avec la distance : épais devant, invisible au
 * fond. Pour obtenir une largeur constante en pixels, l'offset doit croître
 * proportionnellement à la distance et à la taille du champ de vision.
 */

/** Élément [1][1] de la matrice de projection perspective : 1 / tan(fov / 2). */
export function projection11(fovDegres: number): number {
  return 1 / Math.tan((fovDegres * Math.PI) / 360)
}

/**
 * Décalage à appliquer le long de la normale, en unités de scène, pour obtenir
 * un contour de `largeurPx` pixels à l'écran.
 *
 * @param zVue        profondeur du sommet en espace vue (négatif devant la caméra)
 * @param largeurPx   largeur voulue du contour, en pixels
 * @param hauteurEcranPx hauteur du tampon de dessin, en pixels
 * @param p11         élément [1][1] de la matrice de projection
 */
export function offsetContour(
  zVue: number,
  largeurPx: number,
  hauteurEcranPx: number,
  p11: number,
): number {
  return (-zVue * largeurPx * 2) / (hauteurEcranPx * p11)
}
```

- [ ] **Étape 4 : Lancer les tests et vérifier qu'ils passent**

Lancer : `npx vitest run src/noyau/contour.test.ts`
Attendu : SUCCÈS, 8 tests.

- [ ] **Étape 5 : Écrire la page du banc**

`bancs/0a-contour.html` :

```html
<!doctype html>
<html lang="fr">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Banc 0a — contour</title>
    <style>
      html, body { margin: 0; overflow: hidden; background: #F2EEE4; }
      canvas { display: block; }
    </style>
  </head>
  <body>
    <script type="module" src="./0a-contour.ts"></script>
  </body>
</html>
```

- [ ] **Étape 6 : Écrire le banc de la coque inversée**

`bancs/0a-contour.ts` :

```ts
import * as THREE from 'three'
import { creerBanc, demarrerBoucle, definirLignes, mesurerGpu, enregistrerResultat } from './harnais.js'
import { offsetContour, projection11 } from '../src/noyau/contour.js'

const NOMBRE = 100_000
const LARGEUR_CONTOUR_PX = 1.5

// Six familles Okabe-Ito, palette validée de la spec.
const FAMILLES = [0x0072b2, 0x009e73, 0xe69f00, 0xcc79a7, 0x56b4e9, 0xd55e00]

const ctx = creerBanc({ titre: 'Banc 0a — contour par coque inversée' })
ctx.scene.add(new THREE.HemisphereLight(0xffffff, 0x8a7f6d, 2.2))

const geometrie = new THREE.IcosahedronGeometry(0.28, 1)

/** Remplit un cube d'instances aux couleurs de famille, réparties au hasard. */
function poserInstances(maillage: THREE.InstancedMesh): void {
  const matrice = new THREE.Matrix4()
  const couleur = new THREE.Color()
  for (let i = 0; i < NOMBRE; i++) {
    matrice.setPosition(
      (Math.random() - 0.5) * 26,
      (Math.random() - 0.5) * 16,
      (Math.random() - 0.5) * 26,
    )
    maillage.setMatrixAt(i, matrice)
    couleur.setHex(FAMILLES[i % FAMILLES.length]!)
    maillage.setColorAt(i, couleur)
  }
  maillage.instanceMatrix.needsUpdate = true
  if (maillage.instanceColor) maillage.instanceColor.needsUpdate = true
}

// --- Corps des instances ---
const materiauCorps = new THREE.MeshLambertMaterial({ vertexColors: true })
const corps = new THREE.InstancedMesh(geometrie, materiauCorps, NOMBRE)
corps.frustumCulled = false
poserInstances(corps)
ctx.scene.add(corps)

// --- Coque de contour : mêmes matrices, faces arrière, couleur sombre ---
const uniformesContour = {
  largeurPx: { value: LARGEUR_CONTOUR_PX },
  hauteurEcran: { value: window.innerHeight * Math.min(window.devicePixelRatio, 2) },
  p11: { value: projection11(50) },
}

const materiauContour = new THREE.MeshBasicMaterial({ color: 0x2a2320, side: THREE.BackSide })
materiauContour.onBeforeCompile = (shader) => {
  shader.uniforms.largeurPx = uniformesContour.largeurPx
  shader.uniforms.hauteurEcran = uniformesContour.hauteurEcran
  shader.uniforms.p11 = uniformesContour.p11
  shader.vertexShader = shader.vertexShader
    .replace(
      '#include <common>',
      `#include <common>
       uniform float largeurPx;
       uniform float hauteurEcran;
       uniform float p11;`,
    )
    .replace(
      '#include <begin_vertex>',
      `#include <begin_vertex>
       // Position en espace vue, instanciation comprise.
       vec4 posVue = modelViewMatrix * instanceMatrix * vec4(position, 1.0);
       // Normale transformée SANS normalisation : sa longueur donne le facteur
       // d'échelle entre espace objet et espace vue. On évite ainsi inverse(),
       // qui n'existe qu'en GLSL ES 3.00 et échouerait selon la version compilée.
       vec3 normaleVue = mat3(modelViewMatrix) * mat3(instanceMatrix) * normal;
       float echelleVue = max(length(normaleVue), 1e-6);
       // Largeur constante à l'écran : l'offset croît avec la distance.
       float offsetVue = (-posVue.z * largeurPx * 2.0) / (hauteurEcran * p11);
       // Ramené en espace objet, où vit « transformed ».
       transformed += normal * (offsetVue / echelleVue);`,
    )
}

const coque = new THREE.InstancedMesh(geometrie, materiauContour, NOMBRE)
coque.frustumCulled = false
coque.renderOrder = -1
coque.instanceMatrix.copy(corps.instanceMatrix)
coque.instanceMatrix.needsUpdate = true
ctx.scene.add(coque)

window.addEventListener('resize', () => {
  uniformesContour.hauteurEcran.value = window.innerHeight * Math.min(window.devicePixelRatio, 2)
})

// --- Bascules clavier ---
let contourActif = true
let rotationActive = true
const mesures: Array<{ mode: string; gpuMs: number; ips: number }> = []

window.addEventListener('keydown', (e) => {
  if (e.key === 'c') { contourActif = !contourActif; coque.visible = contourActif }
  if (e.key === ' ') rotationActive = !rotationActive
  if (e.key === 'e') {
    mesures.push({
      mode: contourActif ? 'coque inversée' : 'sans contour',
      gpuMs: mesurerGpu() ?? -1,
      ips: ctx.compteur.imagesParSeconde(),
    })
    enregistrerResultat('0a-coque-inversee', mesures)
  }
})

// Vérification du contour intérieur : deux amas serrés de la MÊME teinte.
// C'est le cas que la critique dit impossible — on regarde s'il l'est vraiment.
const materiauTemoin = new THREE.MeshLambertMaterial({ color: FAMILLES[0] })
for (let i = 0; i < 12; i++) {
  const s = new THREE.Mesh(new THREE.IcosahedronGeometry(1.1, 2), materiauTemoin)
  s.position.set(-9 + (i % 4) * 1.3, 6 - Math.floor(i / 4) * 1.3, 12)
  ctx.scene.add(s)
}

demarrerBoucle(ctx, () => {
  if (rotationActive) {
    corps.rotation.y += 0.0015
    coque.rotation.y = corps.rotation.y
  }
  const gpu = mesurerGpu()
  definirLignes([
    `BANC 0a — coque inversée`,
    ``,
    `instances       ${NOMBRE.toLocaleString('fr-FR')} × 2 passes`,
    `contour         ${contourActif ? `actif, ${LARGEUR_CONTOUR_PX} px` : 'coupé'}`,
    `temps GPU       ${gpu === null ? 'extension absente' : gpu.toFixed(2) + ' ms'}`,
    `images/s        ${ctx.compteur.imagesParSeconde().toFixed(0)}`,
    `temps image     ${ctx.compteur.moyenne().toFixed(2)} ms`,
    ``,
    `[c] contour  [espace] rotation  [e] exporter`,
    ``,
    `À juger à l'œil, en haut à gauche : le contour reste-t-il`,
    `de largeur constante du premier plan au fond ? Sépare-t-il`,
    `les douze sphères témoins, toutes de la même teinte ?`,
  ])
})
```

- [ ] **Étape 7 : Mesurer**

Lancer : `npm run banc`, puis ouvrir `http://localhost:5173/bancs/0a-contour.html`.

Relever, en laissant la scène se stabiliser dix secondes à chaque fois :
1. Temps GPU **avec** contour (`c` pour l'activer).
2. Temps GPU **sans** contour.
3. La différence est le coût du contour. **Seuil d'acceptation : ≤ 2 ms.** Au-delà, il consomme plus que la profondeur de champ et le SSAO réunis ne le méritent.

Juger à l'œil, et écrire la réponse :
- La largeur du contour est-elle constante du premier plan au fond ?
- Les douze sphères témoins de même teinte sont-elles séparées les unes des autres ?

Appuyer sur `e` pour exporter les mesures dans `bancs/resultats/`.

- [ ] **Étape 8 : Commiter**

```bash
git add src/noyau/contour.ts src/noyau/contour.test.ts bancs/0a-contour.html bancs/0a-contour.ts
git commit -m "Lot 0 : banc du contour par coque inversée à largeur constante"
```

---

## Tâche 6 : Banc 0a — détection de bord en post-traitement

La seconde voie. Elle est soupçonnée de coûter la classe du SSAO, soit environ 5,6 ms, ce qui la disqualifierait. On mesure au lieu de supposer.

**Fichiers :**
- Modifier : `bancs/0a-contour.ts` (ajouter le mode post-traitement)

**Interfaces :**
- Consomme : tout ce que la tâche 5 a produit.
- Produit : une troisième valeur dans le tableau `mesures`, sous le mode `'détection de bord'`.

- [ ] **Étape 1 : Ajouter la passe de bord**

Insérer dans `bancs/0a-contour.ts`, juste avant l'appel à `demarrerBoucle` :

```ts
// --- Seconde voie : détection de bord sur la profondeur ---
// On rend la scène dans une cible avec sa profondeur, puis on cherche les
// discontinuités. Une passe pleine résolution, comme le SSAO mesuré à 5,63 ms.

const cible = new THREE.WebGLRenderTarget(
  window.innerWidth * Math.min(window.devicePixelRatio, 2),
  window.innerHeight * Math.min(window.devicePixelRatio, 2),
)
cible.depthTexture = new THREE.DepthTexture(cible.width, cible.height)
cible.depthTexture.type = THREE.UnsignedIntType

const sceneBord = new THREE.Scene()
const cameraBord = new THREE.OrthographicCamera(-1, 1, 1, -1, 0, 1)
const materiauBord = new THREE.ShaderMaterial({
  uniforms: {
    couleurs: { value: cible.texture },
    profondeur: { value: cible.depthTexture },
    tailleTexel: { value: new THREE.Vector2(1 / cible.width, 1 / cible.height) },
    proche: { value: ctx.camera.near },
    loin: { value: ctx.camera.far },
  },
  vertexShader: `
    varying vec2 vUv;
    void main() { vUv = uv; gl_Position = vec4(position.xy, 0.0, 1.0); }
  `,
  fragmentShader: `
    varying vec2 vUv;
    uniform sampler2D couleurs;
    uniform sampler2D profondeur;
    uniform vec2 tailleTexel;
    uniform float proche;
    uniform float loin;

    float lineaire(vec2 uv) {
      float z = texture2D(profondeur, uv).x * 2.0 - 1.0;
      return (2.0 * proche * loin) / (loin + proche - z * (loin - proche));
    }

    void main() {
      float c = lineaire(vUv);
      // Sobel réduit à quatre voisins : suffisant pour une silhouette.
      float d = abs(lineaire(vUv + vec2(tailleTexel.x, 0.0)) - c)
              + abs(lineaire(vUv - vec2(tailleTexel.x, 0.0)) - c)
              + abs(lineaire(vUv + vec2(0.0, tailleTexel.y)) - c)
              + abs(lineaire(vUv - vec2(0.0, tailleTexel.y)) - c);
      // Seuil relatif à la profondeur : un bord lointain compte autant qu'un proche.
      float bord = smoothstep(0.004 * c, 0.02 * c, d);
      vec3 rendu = texture2D(couleurs, vUv).rgb;
      gl_FragColor = vec4(mix(rendu, vec3(0.165, 0.137, 0.125), bord), 1.0);
    }
  `,
})
sceneBord.add(new THREE.Mesh(new THREE.PlaneGeometry(2, 2), materiauBord))

let modeBord = false
window.addEventListener('keydown', (e) => {
  if (e.key === 'b') {
    modeBord = !modeBord
    coque.visible = contourActif && !modeBord
  }
  if (e.key === 'x') {
    mesures.push({ mode: 'détection de bord', gpuMs: mesurerGpu() ?? -1, ips: ctx.compteur.imagesParSeconde() })
    enregistrerResultat('0a-detection-bord', mesures)
  }
})

window.addEventListener('resize', () => {
  const r = Math.min(window.devicePixelRatio, 2)
  cible.setSize(window.innerWidth * r, window.innerHeight * r)
  materiauBord.uniforms.tailleTexel.value.set(1 / cible.width, 1 / cible.height)
})
```

- [ ] **Étape 2 : Brancher le rendu en deux passes**

Remplacer l'appel `demarrerBoucle(ctx, ...)` par une version qui rend dans la cible quand le mode bord est actif. Ajouter, **juste avant** l'appel existant à `demarrerBoucle` :

```ts
// Le harnais rend toujours la scène principale ; en mode bord on la redirige
// vers la cible, puis on applique la passe plein écran par-dessus.
const rendreOriginal = ctx.renderer.render.bind(ctx.renderer)
ctx.renderer.render = ((scene: THREE.Scene, camera: THREE.Camera) => {
  if (!modeBord || scene === sceneBord) {
    rendreOriginal(scene, camera)
    return
  }
  ctx.renderer.setRenderTarget(cible)
  rendreOriginal(scene, camera)
  ctx.renderer.setRenderTarget(null)
  rendreOriginal(sceneBord, cameraBord)
}) as typeof ctx.renderer.render
```

Puis ajouter deux lignes au panneau, dans le tableau passé à `definirLignes` :

```ts
    `mode bord       ${modeBord ? 'actif' : 'coupé'}`,
    `[b] bord  [x] exporter le bord`,
```

- [ ] **Étape 3 : Vérifier que TypeScript accepte le fichier**

Lancer : `npx tsc --noEmit`
Attendu : aucune erreur.

- [ ] **Étape 4 : Mesurer**

Lancer : `npm run banc`, ouvrir `http://localhost:5173/bancs/0a-contour.html`.

Relever le temps GPU en mode bord (`b`), à comparer aux deux valeurs de la tâche 5. Juger à l'œil la même chose : largeur constante, et séparation des douze sphères témoins de même teinte — la détection de bord travaillant sur la profondeur, elle devrait les séparer même à couleur identique.

Exporter avec `x`.

- [ ] **Étape 5 : Commiter**

```bash
git add bancs/0a-contour.ts
git commit -m "Lot 0 : banc de la détection de bord en post-traitement"
```

---

## Tâche 7 : Verdict du banc 0a

Un banc sans verdict écrit est un banc perdu. Cette tâche transforme trois nombres en une décision.

**Fichiers :**
- Créer : `docs/superpowers/rapports/lot-0.md` (section 0a uniquement)

**Interfaces :**
- Consomme : les fichiers JSON exportés par les tâches 5 et 6.
- Produit : la section `## 0a — Contour` du rapport, avec un verdict parmi trois valeurs exactes : `coque inversée retenue`, `détection de bord retenue`, `repli`.

- [ ] **Étape 1 : Écrire la section du rapport**

Créer `docs/superpowers/rapports/lot-0.md` avec cette structure, en remplaçant chaque valeur entre chevrons par le chiffre relevé :

```markdown
# Lot 0 — Résultats des trois bancs

Machine de mesure : <modèle exact, navigateur, version>
Date : <date>

## 0a — Contour

| Mode | Temps GPU | Images/s | Surcoût |
|---|---|---|---|
| Sans contour (référence) | <x> ms | <x> | — |
| Coque inversée, 1,5 px | <x> ms | <x> | <x> ms |
| Détection de bord | <x> ms | <x> | <x> ms |

**Largeur constante du premier plan au fond** — coque inversée : <oui/non>. Détection de bord : <oui/non>.

**Séparation des douze sphères témoins de même teinte** — coque inversée : <oui/non>. Détection de bord : <oui/non>.

**Verdict : <coque inversée retenue | détection de bord retenue | repli>**

<Une phrase de justification appuyée sur les chiffres du tableau.>
```

- [ ] **Étape 2 : Appliquer la règle de décision**

Dans l'ordre, sans interprétation :

1. Si la coque inversée coûte **≤ 2 ms** *et* garde une largeur constante *et* sépare les sphères témoins → `coque inversée retenue`.
2. Sinon, si la détection de bord coûte **≤ 2 ms** *et* remplit les deux mêmes critères → `détection de bord retenue`.
3. Sinon → `repli`. Appliquer alors le repli nommé en §5.3 de la spec, dans cet ordre :
   - d'abord séparer les six familles par la luminosité et la silhouette seules, sans contour ;
   - si l'écart de luminosité ne suffit pas à distinguer six familles, basculer sur la direction « coupe optique » (fond `#07080B`, canaux `#3B6FD4`, `#22A05A`, `#C93E9E`, `#C08420`), déjà validée intégralement.

En cas de `repli`, ajouter au rapport une ligne indiquant laquelle des deux options est retenue et pourquoi.

- [ ] **Étape 3 : Commiter**

```bash
git add docs/superpowers/rapports/lot-0.md
git commit -m "Lot 0 : verdict du banc 0a sur le contour"
```

---

## Tâche 8 : Calcul de remplissage d'une dalle

Le cœur du banc 0b. Combien d'objets faut-il pour atteindre une occupation volumique donnée, et quelle arête de dalle le budget autorise-t-il ? C'est de l'arithmétique, elle se teste sans GPU, et c'est elle qui a révélé que le curseur de densité était infaisable à l'échelle de la cellule.

**Fichiers :**
- Créer : `src/noyau/densite.ts`
- Test : `src/noyau/densite.test.ts`

**Interfaces :**
- Consomme : rien.
- Produit :
  - `VOLUME_COMPLEXE_NM3: number` — volume de référence d'un complexe reconnaissable, en nm³.
  - `OCCUPATION_CYTOSOL: number` — fraction volumique occupée par les macromolécules.
  - `nombreObjets(areteNm: number, profondeurNm: number, occupation: number, volumeObjetNm3: number): number`
  - `areteTenable(budgetObjets: number, profondeurNm: number, occupation: number, volumeObjetNm3: number): number`
  - `objetsPourCelluleEntiere(volumeCelluleUm3: number, occupation: number, volumeObjetNm3: number): number`

- [ ] **Étape 1 : Écrire les tests qui échouent**

`src/noyau/densite.test.ts` :

```ts
import { describe, it, expect } from 'vitest'
import {
  VOLUME_COMPLEXE_NM3,
  OCCUPATION_CYTOSOL,
  nombreObjets,
  areteTenable,
  objetsPourCelluleEntiere,
} from './densite.js'

describe('constantes de référence', () => {
  it('prend un complexe reconnaissable de l’ordre du ribosome', () => {
    expect(VOLUME_COMPLEXE_NM3).toBeGreaterThanOrEqual(2_500)
    expect(VOLUME_COMPLEXE_NM3).toBeLessThanOrEqual(3_000)
  })

  it('retient une occupation dans la fourchette mesurée de 20 à 30 %', () => {
    expect(OCCUPATION_CYTOSOL).toBeGreaterThanOrEqual(0.2)
    expect(OCCUPATION_CYTOSOL).toBeLessThanOrEqual(0.3)
  })
})

describe('nombre d’objets dans une dalle', () => {
  it('donne environ 90 000 complexes par micromètre cube à 25 %', () => {
    const n = nombreObjets(1_000, 1_000, 0.25, 2_750)
    expect(n).toBeGreaterThan(80_000)
    expect(n).toBeLessThan(100_000)
  })

  it('croît comme le carré de l’arête à profondeur fixée', () => {
    const petit = nombreObjets(500, 200, 0.25, 2_750)
    const grand = nombreObjets(1_000, 200, 0.25, 2_750)
    expect(grand / petit).toBeCloseTo(4, 6)
  })

  it('croît proportionnellement à la profondeur', () => {
    const mince = nombreObjets(1_000, 100, 0.25, 2_750)
    const epaisse = nombreObjets(1_000, 300, 0.25, 2_750)
    expect(epaisse / mince).toBeCloseTo(3, 6)
  })

  it('renvoie un entier', () => {
    expect(Number.isInteger(nombreObjets(777, 213, 0.25, 2_750))).toBe(true)
  })

  it('refuse une occupation hors de zéro à un', () => {
    expect(() => nombreObjets(1_000, 200, 1.5, 2_750)).toThrow(/occupation/i)
    expect(() => nombreObjets(1_000, 200, -0.1, 2_750)).toThrow(/occupation/i)
  })
})

describe('arête tenable pour un budget donné', () => {
  it('est l’inverse exact du calcul de nombre', () => {
    const arete = areteTenable(200_000, 300, 0.25, 2_750)
    expect(nombreObjets(arete, 300, 0.25, 2_750)).toBeLessThanOrEqual(200_000)
    expect(nombreObjets(arete + 1, 300, 0.25, 2_750)).toBeGreaterThan(200_000)
  })

  it('tombe dans la fourchette annoncée par la spec sur un budget de bureau', () => {
    // Dalle à profondeur bornée : 300 nm suffisent, au-delà tout est occulté.
    const arete = areteTenable(200_000, 300, 0.25, 2_750)
    expect(arete).toBeGreaterThanOrEqual(300)
    expect(arete).toBeLessThanOrEqual(1_500)
  })

  it('rétrécit sur un budget mobile', () => {
    const bureau = areteTenable(200_000, 300, 0.25, 2_750)
    const mobile = areteTenable(40_000, 300, 0.25, 2_750)
    expect(mobile).toBeLessThan(bureau)
  })
})

describe('le mur qui interdit le curseur de densité à l’échelle de la cellule', () => {
  it('demande au moins cent millions d’objets pour une cellule entière', () => {
    const n = objetsPourCelluleEntiere(2_000, 0.25, 2_750)
    expect(n).toBeGreaterThan(1e8)
  })

  it('dépasse le budget de rendu d’au moins trois ordres de grandeur', () => {
    const n = objetsPourCelluleEntiere(2_000, 0.25, 2_750)
    expect(n / 200_000).toBeGreaterThan(1_000)
  })
})
```

- [ ] **Étape 2 : Lancer les tests et vérifier qu'ils échouent**

Lancer : `npx vitest run src/noyau/densite.test.ts`
Attendu : ÉCHEC, `Failed to resolve import "./densite.js"`.

- [ ] **Étape 3 : Écrire l'implémentation**

`src/noyau/densite.ts` :

```ts
/**
 * Arithmétique de l'encombrement moléculaire.
 *
 * C'est ce calcul qui a montré qu'un curseur de densité à l'échelle de la
 * cellule entière est physiquement infaisable : une cellule mammifère demande
 * entre 10⁸ et 10¹⁰ objets là où le budget de rendu en autorise ~2 × 10⁵.
 * L'encombrement vrai n'existe donc que dans une dalle sub-micronique.
 */

/**
 * Volume d'un complexe reconnaissable, en nm³.
 *
 * L'unité rendue retenue est le complexe qui a une silhouette identifiable,
 * pas le monomère : la silhouette porte l'information. Ce choix change le
 * volume finançable d'un facteur 60.
 */
export const VOLUME_COMPLEXE_NM3 = 2_750

/** Fraction volumique du cytosol occupée par des macromolécules (20 à 30 % mesurés). */
export const OCCUPATION_CYTOSOL = 0.25

function verifierOccupation(occupation: number): void {
  if (!(occupation > 0 && occupation <= 1)) {
    throw new Error(`occupation invalide : ${occupation} (attendu entre 0 exclu et 1 inclus)`)
  }
}

/** Nombre d'objets à instancier pour remplir une dalle à l'occupation voulue. */
export function nombreObjets(
  areteNm: number,
  profondeurNm: number,
  occupation: number,
  volumeObjetNm3: number,
): number {
  verifierOccupation(occupation)
  const volumeDalle = areteNm * areteNm * profondeurNm
  return Math.floor((volumeDalle * occupation) / volumeObjetNm3)
}

/** Plus grande arête de dalle tenable pour un budget d'instances donné. */
export function areteTenable(
  budgetObjets: number,
  profondeurNm: number,
  occupation: number,
  volumeObjetNm3: number,
): number {
  verifierOccupation(occupation)
  const surface = (budgetObjets * volumeObjetNm3) / (occupation * profondeurNm)
  return Math.floor(Math.sqrt(surface))
}

/** Nombre d'objets qu'exigerait une cellule entière — le chiffre du mur. */
export function objetsPourCelluleEntiere(
  volumeCelluleUm3: number,
  occupation: number,
  volumeObjetNm3: number,
): number {
  verifierOccupation(occupation)
  const volumeNm3 = volumeCelluleUm3 * 1e9 // 1 µm³ = 10⁹ nm³
  return Math.floor((volumeNm3 * occupation) / volumeObjetNm3)
}
```

- [ ] **Étape 4 : Lancer les tests et vérifier qu'ils passent**

Lancer : `npx vitest run src/noyau/densite.test.ts`
Attendu : SUCCÈS, 12 tests.

- [ ] **Étape 5 : Commiter**

```bash
git add src/noyau/densite.ts src/noyau/densite.test.ts
git commit -m "Lot 0 : arithmétique de l'encombrement et de l'arête tenable"
```

---

## Tâche 9 : Banc 0b — mesure de la dalle sur ordinateur

**Fichiers :**
- Créer : `bancs/0b-dalle.html`
- Créer : `bancs/0b-dalle.ts`

**Interfaces :**
- Consomme : `creerBanc`, `demarrerBoucle`, `definirLignes`, `mesurerGpu`, `enregistrerResultat` ; `nombreObjets`, `VOLUME_COMPLEXE_NM3`, `OCCUPATION_CYTOSOL` ; `nmVersUnites`.
- Produit : un fichier `0b-dalle.json` listant, pour chaque arête essayée, le nombre d'instances, le temps GPU et les images par seconde.

- [ ] **Étape 1 : Écrire la page**

`bancs/0b-dalle.html` :

```html
<!doctype html>
<html lang="fr">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Banc 0b — dalle</title>
    <style>
      html, body { margin: 0; overflow: hidden; background: #F2EEE4; }
      canvas { display: block; }
    </style>
  </head>
  <body>
    <script type="module" src="./0b-dalle.ts"></script>
  </body>
</html>
```

- [ ] **Étape 2 : Écrire le banc**

`bancs/0b-dalle.ts` :

```ts
import * as THREE from 'three'
import { creerBanc, demarrerBoucle, definirLignes, mesurerGpu, enregistrerResultat } from './harnais.js'
import { nombreObjets, VOLUME_COMPLEXE_NM3, OCCUPATION_CYTOSOL } from '../src/noyau/densite.js'
import { nmVersUnites } from '../src/noyau/echelles.js'

/** Arêtes de dalle à essayer, en nanomètres. Fourchette annoncée par la spec. */
const ARETES_NM = [300, 500, 700, 1_000, 1_500]

/** Profondeur de la dalle. Au-delà, tout est occulté par la première couche. */
const PROFONDEUR_NM = 300

const FAMILLES = [0x0072b2, 0x009e73, 0xe69f00, 0xcc79a7, 0x56b4e9, 0xd55e00]

const ctx = creerBanc({ titre: 'Banc 0b — dalle à densité vraie' })
ctx.scene.add(new THREE.HemisphereLight(0xffffff, 0x8a7f6d, 2.2))

let indexArete = 0
let amas: THREE.InstancedMesh | null = null
const mesures: Array<{ areteNm: number; instances: number; gpuMs: number; ips: number }> = []

// Objets temporaires hissés hors de toute boucle : aucune allocation par image.
const matriceTemp = new THREE.Matrix4()
const quaternionTemp = new THREE.Quaternion()
const positionTemp = new THREE.Vector3()
const echelleTemp = new THREE.Vector3()
const couleurTemp = new THREE.Color()

function construireDalle(areteNm: number): void {
  if (amas) {
    ctx.scene.remove(amas)
    amas.geometry.dispose()
    ;(amas.material as THREE.Material).dispose()
    amas.dispose()
  }

  const instances = nombreObjets(areteNm, PROFONDEUR_NM, OCCUPATION_CYTOSOL, VOLUME_COMPLEXE_NM3)

  // Le champ visible est l'arête : la dalle remplit toujours l'écran.
  const areteUnites = nmVersUnites(areteNm, areteNm)
  const profondeurUnites = nmVersUnites(PROFONDEUR_NM, areteNm)

  // Rayon d'une sphère de volume VOLUME_COMPLEXE_NM3, converti en unités.
  const rayonNm = Math.cbrt((3 * VOLUME_COMPLEXE_NM3) / (4 * Math.PI))
  const rayonUnites = nmVersUnites(rayonNm, areteNm)

  const geometrie = new THREE.IcosahedronGeometry(rayonUnites, 1)
  const materiau = new THREE.MeshLambertMaterial({ vertexColors: true })
  amas = new THREE.InstancedMesh(geometrie, materiau, instances)
  amas.frustumCulled = false

  for (let i = 0; i < instances; i++) {
    positionTemp.set(
      (Math.random() - 0.5) * areteUnites,
      (Math.random() - 0.5) * areteUnites,
      (Math.random() - 0.5) * profondeurUnites,
    )
    quaternionTemp.set(Math.random(), Math.random(), Math.random(), Math.random()).normalize()
    // Les complexes réels ne sont pas tous de même taille : on varie de ±25 %.
    const f = 0.75 + Math.random() * 0.5
    echelleTemp.set(f, f, f)
    matriceTemp.compose(positionTemp, quaternionTemp, echelleTemp)
    amas.setMatrixAt(i, matriceTemp)
    couleurTemp.setHex(FAMILLES[i % FAMILLES.length]!)
    amas.setColorAt(i, couleurTemp)
  }
  amas.instanceMatrix.needsUpdate = true
  if (amas.instanceColor) amas.instanceColor.needsUpdate = true
  ctx.scene.add(amas)

  ctx.camera.position.set(0, 0, areteUnites * 0.62)
}

construireDalle(ARETES_NM[0]!)

window.addEventListener('keydown', (e) => {
  if (e.key === 'ArrowRight' && indexArete < ARETES_NM.length - 1) {
    indexArete++
    construireDalle(ARETES_NM[indexArete]!)
  }
  if (e.key === 'ArrowLeft' && indexArete > 0) {
    indexArete--
    construireDalle(ARETES_NM[indexArete]!)
  }
  if (e.key === 'e') {
    mesures.push({
      areteNm: ARETES_NM[indexArete]!,
      instances: amas!.count,
      gpuMs: mesurerGpu() ?? -1,
      ips: ctx.compteur.imagesParSeconde(),
    })
    enregistrerResultat('0b-dalle', mesures)
  }
})

demarrerBoucle(ctx, () => {
  const areteNm = ARETES_NM[indexArete]!
  const ips = ctx.compteur.imagesParSeconde()
  const gpu = mesurerGpu()
  definirLignes([
    `BANC 0b — dalle à densité vraie`,
    ``,
    `arête           ${areteNm} nm`,
    `profondeur      ${PROFONDEUR_NM} nm`,
    `occupation      ${(OCCUPATION_CYTOSOL * 100).toFixed(0)} %`,
    `instances       ${amas!.count.toLocaleString('fr-FR')}`,
    `temps GPU       ${gpu === null ? 'extension absente' : gpu.toFixed(2) + ' ms'}`,
    `images/s        ${ips.toFixed(0)}`,
    ``,
    `porte bureau    ${ips >= 55 ? 'PASSÉE' : 'ÉCHOUÉE'} (≥ 55)`,
    `porte mobile    ${ips >= 28 ? 'PASSÉE' : 'ÉCHOUÉE'} (≥ 28)`,
    ``,
    `[← →] changer d'arête   [e] exporter`,
  ])
})
```

- [ ] **Étape 3 : Vérifier que TypeScript accepte le fichier**

Lancer : `npx tsc --noEmit`
Attendu : aucune erreur.

- [ ] **Étape 4 : Mesurer sur le Mac**

Lancer : `npm run banc`, ouvrir `http://localhost:5173/bancs/0b-dalle.html`.

Parcourir les cinq arêtes avec les flèches, laisser dix secondes à chacune, appuyer sur `e` à chaque palier. **Noter la plus grande arête qui garde ≥ 55 images par seconde.**

- [ ] **Étape 5 : Commiter**

```bash
git add bancs/0b-dalle.html bancs/0b-dalle.ts
git commit -m "Lot 0 : banc de la dalle à densité vraie"
```

---

## Tâche 10 : Banc 0b — mesure sur un téléphone réel

Aucun appareil mobile n'a jamais été mesuré sur ce projet, et l'émulation du navigateur ne simule pas le GPU. Extrapoler serait de la fabrication. Cette tâche est la seule qui produise un chiffre mobile honnête.

**Fichiers :**
- Modifier : `vite.config.ts` (exposer le serveur sur le réseau local)

**Interfaces :**
- Consomme : le banc de la tâche 9, inchangé.
- Produit : une ligne mobile dans le rapport, et l'arête tenable sur téléphone.

- [ ] **Étape 1 : Exposer le serveur sur le réseau local**

Ajouter à `vite.config.ts`, dans l'objet passé à `defineConfig` :

```ts
  server: { host: true },
```

- [ ] **Étape 2 : Lancer et se connecter depuis le téléphone**

```bash
npm run banc
```

Vite affiche une adresse `Network:` du type `http://192.168.x.x:5173/`. Ouvrir sur le téléphone, sur le même réseau Wi-Fi, la page `/bancs/0b-dalle.html`.

Si l'adresse n'apparaît pas, récupérer l'IP de la machine :

```bash
ipconfig getifaddr en0
```

- [ ] **Étape 3 : Mesurer sur au moins deux appareils**

Un iPhone récent et un Android milieu de gamme. Pour chacun :

1. Parcourir les arêtes en touchant les côtés de l'écran — ou, si les flèches ne sont pas accessibles, modifier `ARETES_NM` pour n'en garder qu'une et recharger.
2. **Laisser tourner cinq minutes sur l'arête retenue.** Le throttling thermique est réel : une scène stable en une minute peut perdre 30 à 40 % ensuite. Relever les images par seconde à une minute *et* à cinq minutes.
3. Noter si le panneau affiche « CONTEXTE WEBGL PERDU ». C'est le mode de panne typique d'iOS par dépassement mémoire, et il doit être consigné même s'il ne survient qu'une fois.

**Noter la plus grande arête qui garde ≥ 28 images par seconde à cinq minutes**, pas à une minute.

- [ ] **Étape 4 : Commiter**

```bash
git add vite.config.ts
git commit -m "Lot 0 : exposer le serveur de bancs sur le réseau local pour les mesures mobiles"
```

---

## Tâche 11 : Banc 0c — production d'une silhouette moléculaire

Le poste caché le plus important du projet. On chronomètre la production d'une silhouette réelle, de bout en bout, sur la molécule vedette du lot 1 : la pompe Na⁺/K⁺-ATPase.

**Fichiers :**
- Créer : `src/noyau/pdb.ts`
- Test : `src/noyau/pdb.test.ts`
- Créer : `outils/silhouette.ts`

**Interfaces :**
- Consomme : rien de nos modules.
- Produit :
  - `interface AtomeCa { x: number; y: number; z: number; chaine: string }`
  - `analyserCa(texte: string): AtomeCa[]` — extrait les carbones alpha d'un fichier PDB.
  - `centrerEtNormaliser(atomes: AtomeCa[]): Float32Array` — coordonnées centrées, mises à l'échelle dans une sphère de rayon 1, à plat en `[x, y, z, x, y, z, ...]`.
  - `outils/silhouette.ts` exécutable par `npx tsx` : télécharge un PDB, écrit un `.bin`.

- [ ] **Étape 1 : Écrire les tests qui échouent**

`src/noyau/pdb.test.ts` :

```ts
import { describe, it, expect } from 'vitest'
import { analyserCa, centrerEtNormaliser } from './pdb.js'

// Extrait réel au format PDB : colonnes à position fixe.
const EXTRAIT = [
  'ATOM      1  N   MET A   1      38.428  15.115  11.334  1.00 42.15           N',
  'ATOM      2  CA  MET A   1      37.223  14.298  11.539  1.00 41.03           C',
  'ATOM      3  C   MET A   1      36.017  15.148  11.906  1.00 40.22           C',
  'ATOM      4  CA  GLY A   2      34.812  14.331  12.111  1.00 39.88           C',
  'ATOM      5  CA  ALA B   1      30.100  10.000   9.000  1.00 38.00           C',
  'HETATM  600  O   HOH A 301      20.000  20.000  20.000  1.00 30.00           O',
  'END',
].join('\n')

describe('analyse des carbones alpha', () => {
  it('ne garde que les atomes CA', () => {
    const atomes = analyserCa(EXTRAIT)
    expect(atomes).toHaveLength(3)
  })

  it('lit les coordonnées aux bonnes colonnes', () => {
    const [premier] = analyserCa(EXTRAIT)
    expect(premier!.x).toBeCloseTo(37.223, 3)
    expect(premier!.y).toBeCloseTo(14.298, 3)
    expect(premier!.z).toBeCloseTo(11.539, 3)
  })

  it('retient la chaîne, pour pouvoir isoler une sous-unité', () => {
    const chaines = analyserCa(EXTRAIT).map((a) => a.chaine)
    expect(chaines).toEqual(['A', 'A', 'B'])
  })

  it('ignore les HETATM, qui sont surtout de l’eau et des ligands', () => {
    expect(analyserCa(EXTRAIT).every((a) => a.x !== 20)).toBe(true)
  })

  it('renvoie un tableau vide sur un texte sans atome', () => {
    expect(analyserCa('END\n')).toEqual([])
  })
})

describe('centrage et normalisation', () => {
  it('produit trois flottants par atome', () => {
    const plat = centrerEtNormaliser(analyserCa(EXTRAIT))
    expect(plat).toBeInstanceOf(Float32Array)
    expect(plat.length).toBe(3 * 3)
  })

  it('centre le nuage sur l’origine', () => {
    const plat = centrerEtNormaliser(analyserCa(EXTRAIT))
    let sx = 0
    for (let i = 0; i < plat.length; i += 3) sx += plat[i]!
    expect(sx / (plat.length / 3)).toBeCloseTo(0, 5)
  })

  it('fait tenir tous les atomes dans la sphère de rayon 1', () => {
    const plat = centrerEtNormaliser(analyserCa(EXTRAIT))
    let maxi = 0
    for (let i = 0; i < plat.length; i += 3) {
      maxi = Math.max(maxi, Math.hypot(plat[i]!, plat[i + 1]!, plat[i + 2]!))
    }
    expect(maxi).toBeCloseTo(1, 5)
  })

  it('refuse un nuage vide plutôt que de diviser par zéro', () => {
    expect(() => centrerEtNormaliser([])).toThrow(/aucun atome/i)
  })
})
```

- [ ] **Étape 2 : Lancer les tests et vérifier qu'ils échouent**

Lancer : `npx vitest run src/noyau/pdb.test.ts`
Attendu : ÉCHEC, `Failed to resolve import "./pdb.js"`.

- [ ] **Étape 3 : Écrire l'implémentation**

`src/noyau/pdb.ts` :

```ts
/**
 * Extraction d'une silhouette moléculaire depuis un fichier PDB.
 *
 * On ne garde que les carbones alpha : quelques centaines de points suffisent
 * à porter la forme reconnaissable d'une protéine, et c'est la silhouette qui
 * porte l'information, pas le détail atomique. Les données PDB sont en CC0.
 *
 * Le format PDB est à colonnes fixes, pas à séparateurs — découper sur les
 * espaces casse dès qu'un champ déborde sur son voisin.
 */

export interface AtomeCa {
  x: number
  y: number
  z: number
  chaine: string
}

export function analyserCa(texte: string): AtomeCa[] {
  const atomes: AtomeCa[] = []
  for (const ligne of texte.split('\n')) {
    if (!ligne.startsWith('ATOM')) continue
    // Colonnes 13-16 : nom de l'atome. « CA  » avec les espaces du format.
    if (ligne.slice(12, 16).trim() !== 'CA') continue
    atomes.push({
      x: Number.parseFloat(ligne.slice(30, 38)),
      y: Number.parseFloat(ligne.slice(38, 46)),
      z: Number.parseFloat(ligne.slice(46, 54)),
      chaine: ligne.slice(21, 22),
    })
  }
  return atomes
}

/** Centre le nuage sur l'origine et le met à l'échelle dans une sphère de rayon 1. */
export function centrerEtNormaliser(atomes: AtomeCa[]): Float32Array {
  if (atomes.length === 0) throw new Error('aucun atome à normaliser')

  let cx = 0
  let cy = 0
  let cz = 0
  for (const a of atomes) {
    cx += a.x
    cy += a.y
    cz += a.z
  }
  cx /= atomes.length
  cy /= atomes.length
  cz /= atomes.length

  let rayonMax = 0
  for (const a of atomes) {
    rayonMax = Math.max(rayonMax, Math.hypot(a.x - cx, a.y - cy, a.z - cz))
  }
  const facteur = rayonMax === 0 ? 1 : 1 / rayonMax

  const plat = new Float32Array(atomes.length * 3)
  atomes.forEach((a, i) => {
    plat[i * 3] = (a.x - cx) * facteur
    plat[i * 3 + 1] = (a.y - cy) * facteur
    plat[i * 3 + 2] = (a.z - cz) * facteur
  })
  return plat
}
```

- [ ] **Étape 4 : Lancer les tests et vérifier qu'ils passent**

Lancer : `npx vitest run src/noyau/pdb.test.ts`
Attendu : SUCCÈS, 9 tests.

- [ ] **Étape 5 : Écrire l'outil de production**

`outils/silhouette.ts` :

```ts
/**
 * Produit un binaire de silhouette à partir d'un identifiant PDB.
 *
 * Usage : npx tsx outils/silhouette.ts 2ZXE
 *
 * Les coordonnées PDB sont sous licence CC0, réutilisables sans attribution.
 * Attention en revanche aux ILLUSTRATIONS Goodsell et Molecule of the Month,
 * qui sont en CC-BY-4.0 : toute image qui devient texture ou tracé de
 * référence impose une attribution dans la page livrée.
 */
import { writeFile, mkdir } from 'node:fs/promises'
import { analyserCa, centrerEtNormaliser } from '../src/noyau/pdb.js'

const identifiant = process.argv[2]
if (!identifiant) {
  console.error('usage : npx tsx outils/silhouette.ts <identifiant PDB>  (exemple : 2ZXE)')
  process.exit(1)
}

const debut = Date.now()

const reponse = await fetch(`https://files.rcsb.org/download/${identifiant.toUpperCase()}.pdb`)
if (!reponse.ok) {
  console.error(`téléchargement impossible : ${reponse.status} ${reponse.statusText}`)
  process.exit(1)
}
const texte = await reponse.text()

const atomes = analyserCa(texte)
if (atomes.length === 0) {
  console.error('aucun carbone alpha trouvé — structure probablement en format mmCIF uniquement')
  process.exit(1)
}

const plat = centrerEtNormaliser(atomes)

await mkdir('assets/silhouettes', { recursive: true })
const chemin = `assets/silhouettes/${identifiant.toLowerCase()}.bin`
await writeFile(chemin, Buffer.from(plat.buffer))

const chaines = new Set(atomes.map((a) => a.chaine))
console.log(`silhouette écrite    ${chemin}`)
console.log(`carbones alpha       ${atomes.length}`)
console.log(`chaînes              ${[...chaines].sort().join(', ')}`)
console.log(`poids                ${(plat.byteLength / 1024).toFixed(1)} Ko`)
console.log(`durée                ${((Date.now() - debut) / 1000).toFixed(1)} s`)
```

- [ ] **Étape 6 : Chronométrer la production réelle**

```bash
npm install --save-dev --save-exact tsx@4.20.3
npx tsx outils/silhouette.ts 2ZXE
```

`2ZXE` est la structure de la Na⁺/K⁺-ATPase, la molécule vedette du lot 1.

**Chronométrer soi-même, montre en main, le temps total** depuis « je veux une silhouette de cette protéine » jusqu'à « j'ai un binaire utilisable » — y compris le temps passé à trouver le bon identifiant PDB, à comprendre quelles chaînes garder, et à juger si la forme obtenue est reconnaissable. Le chiffre affiché par le script ne mesure que le téléchargement et l'analyse ; c'est le temps humain qui est le poste caché.

Répéter sur une deuxième molécule pour avoir une moyenne plutôt qu'un point : `npx tsx outils/silhouette.ts 4V6X` (ribosome humain).

- [ ] **Étape 7 : Commiter**

```bash
git add src/noyau/pdb.ts src/noyau/pdb.test.ts outils/silhouette.ts package.json package-lock.json
echo 'assets/silhouettes/' >> .gitignore
git add .gitignore
git commit -m "Lot 0 : extraction d'une silhouette moléculaire depuis un fichier PDB"
```

---

## Tâche 12 : Rapport du lot 0 et décisions

**Fichiers :**
- Modifier : `docs/superpowers/rapports/lot-0.md` (compléter avec 0b et 0c)

**Interfaces :**
- Consomme : les fichiers JSON exportés et les chronométrages des tâches 5, 6, 9, 10, 11.
- Produit : trois verdicts qui débloquent le lot 1.

- [ ] **Étape 1 : Lancer la suite complète et confirmer qu'elle est verte**

Lancer : `npm test`
Attendu : SUCCÈS, 48 tests répartis sur 6 fichiers.

Lancer : `npx tsc --noEmit`
Attendu : aucune erreur.

- [ ] **Étape 2 : Compléter le rapport**

Ajouter à `docs/superpowers/rapports/lot-0.md`, à la suite de la section 0a :

```markdown
## 0b — Dalle à densité vraie

Profondeur de dalle : 300 nm. Occupation : 25 %. Unité rendue : complexe reconnaissable, 2 750 nm³.

| Arête | Instances | Mac — GPU | Mac — images/s | iPhone — images/s (1 min) | iPhone — images/s (5 min) | Android — images/s (5 min) |
|---|---|---|---|---|---|---|
| 300 nm | <x> | <x> ms | <x> | <x> | <x> | <x> |
| 500 nm | <x> | <x> ms | <x> | <x> | <x> | <x> |
| 700 nm | <x> | <x> ms | <x> | <x> | <x> | <x> |
| 1 000 nm | <x> | <x> ms | <x> | <x> | <x> | <x> |
| 1 500 nm | <x> | <x> ms | <x> | <x> | <x> | <x> |

Perte de contexte WebGL observée : <jamais / sur quel appareil, à quelle arête>.

**Arête retenue sur ordinateur (≥ 55 images/s) : <x> nm**
**Arête retenue sur mobile (≥ 28 images/s à cinq minutes) : <x> nm**

Ces deux arêtes deviennent des constantes de la spec. Rappel de la règle : quand le budget
manque, **on rétrécit l'arête, jamais la densité** — une boîte de vérité à densité réduite
n'est plus une boîte de vérité.

## 0c — Production d'une silhouette

| Molécule | Identifiant | Carbones alpha | Poids | Temps machine | Temps humain total |
|---|---|---|---|---|---|
| Na⁺/K⁺-ATPase | 2ZXE | <x> | <x> Ko | <x> s | <x> min |
| Ribosome humain | 4V6X | <x> | <x> Ko | <x> s | <x> min |

**Coût moyen d'une silhouette : <x> minutes.**

Nombre de familles moléculaires distinctes prévues sur les quatre lots : <x>.
Coût total extrapolé : <x> heures.

**Verdict : <tenable | réduire le nombre de familles>**

Si le coût total dépasse cinq jours-homme, réduire le nombre de familles distinctes
avant d'engager les quatre phénomènes — c'est moins coûteux que de le découvrir au lot 3.

## Conséquences pour la spec

<Lister les constantes de la spec que ces mesures modifient : arête de la dalle sur
ordinateur et sur mobile, technique de contour retenue, nombre de familles.>
```

- [ ] **Étape 3 : Reporter les décisions dans la spec**

Modifier `docs/superpowers/specs/2026-07-30-cellule-vivante-design.md` :

- §3.4, ligne de la bande 2 : remplacer `0,3 – 1,5 µm` par l'arête effectivement retenue.
- §5.3 : remplacer le paragraphe « risque ouvert » par la technique retenue, ou par le repli appliqué.
- §9.5 : remplacer « aucun appareil mobile n'a été mesuré » par les chiffres relevés.
- §16 « Ce qui reste ouvert » : retirer les trois entrées correspondant aux portes 0a, 0b et 0c.

- [ ] **Étape 4 : Commiter**

```bash
git add docs/superpowers/rapports/lot-0.md docs/superpowers/specs/2026-07-30-cellule-vivante-design.md
git commit -m "Lot 0 : rapport des trois bancs et report des décisions dans la spec"
```

---

## Auto-revue de ce plan

**Couverture de la spec.** Le plan couvre les trois portes du §11 de la spec : 0a par les tâches 5 à 7, 0b par les tâches 8 à 10, 0c par la tâche 11. Les contraintes globales de la spec qui s'appliquent au lot 0 sont reprises en tête : versions épinglées, pureté de `src/noyau/`, interdiction de `THREE.Points`, absence d'allocation en boucle, cibles de 55 et 28 images par seconde. Ce que le plan ne couvre pas, volontairement : tout ce qui relève des lots 1 à 4 — moteur de membrane, bandes d'échelle enchaînées, contenu pédagogique, accessibilité. Ce sont des lots distincts avec leurs propres cycles.

**Absence de réserves.** Aucune étape ne dit « à implémenter plus tard » ni « ajouter la gestion d'erreur appropriée ». Chaque étape de code contient le code. Les valeurs entre chevrons du rapport final sont des emplacements de mesure à remplir, pas des réserves d'implémentation : ce sont précisément les nombres que les bancs produisent.

**Cohérence des types.** `Compteur` est défini en tâche 3 et consommé par le harnais en tâche 4 sous le même nom. `offsetContour` et `projection11` sont définis en tâche 5 et utilisés dans le shader de la même tâche avec la même formule. `nombreObjets`, `VOLUME_COMPLEXE_NM3` et `OCCUPATION_CYTOSOL` sont définis en tâche 8 et consommés en tâche 9 sous ces noms exacts. `nmVersUnites` et `CHAMPS_NM` viennent de la tâche 2 et sont utilisés aux tâches 5 et 9. `analyserCa` et `centrerEtNormaliser` sont définis en tâche 11 et consommés par l'outil de la même tâche.

**Compte de tests attendu** : 1 (tâche 1) + 8 (tâche 2) + 10 (tâche 3) + 8 (tâche 5) + 12 (tâche 8) + 9 (tâche 11) = **48 tests sur 6 fichiers**, chiffre repris à l'identique dans l'étape 1 de la tâche 12.

**Deux corrections appliquées à la relecture.**

La première est cosmétique : trois comptes de tests annoncés ne correspondaient pas au nombre de cas réellement écrits.

La seconde ne l'est pas. Le shader du contour utilisait `inverse()` pour ramener la position décalée de l'espace vue vers l'espace objet. Cette fonction n'existe qu'en GLSL ES 3.00, et selon la version de shader que Three.js compile, la compilation aurait échoué — probablement en silence, avec un contour simplement absent et un banc qui aurait mesuré la mauvaise chose. La version retenue n'inverse rien : elle lit le facteur d'échelle dans la longueur de la normale transformée non normalisée, ce qui est valide dans les deux versions de GLSL et coûte moins cher.
